import CategoriesComponent from "~/components/organisms/CategoriesComponent";

export default function Categories() {
	// !! IMPORTANT if it doesn't load, right-click on chrome refresh icon and select "clear cache and hard reload", or run in private mode. See https://github.com/solidjs/solid-start/issues/1194
	return (
		<section>
			<CategoriesComponent />
		</section>
	);
}
